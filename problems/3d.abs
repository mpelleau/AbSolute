
/* a simple 3d example */

init {
  real x = [0;50];
  real y = [0;50];
  real z = [0;50];
}

constraints {
  (x-20)^2 + (y-20)^2 + (z-20)^2 <= 400;
  (x-20)^2 + (y-20)^2 + (z-20)^2 >= 324;
  x<=20;
}

# -----------------------------------------------------------------
#  Reference :
# -----------------------------------------------------------------


# Domains
var x >= -1000, <= 1000;
var y >= -1000, <= 1000;
var z >= -1000, <= 1000;

subject to
cons1 : z*x^2 - 0.5*x = y^2;
cons2 : z*y^2 + 2*x = -0.5;
cons3 : z - 16*x^2 - 4*x*y^2 = 1;

#solve;
#display x, y, z;

/* root test */

init {
  real x = [-100;100];
  real y = [-100;100];
}

constraints {
  y < cos(x) + 1;
  y > cos(x) - 1;
}

/*solutions{
  {y=0; x=0};
  {y=1; x=1};
  !{y=-1; x=-1};
  !{y=3; x=3};
  !{y=2; x=2};}*/